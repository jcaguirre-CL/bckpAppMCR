const nodemailer=require('nodemailer');const transport=nodemailer.createTransport({service:'Gmail',auth:{user:'',pass:'',},});module.exports=function sendEmail(cc,subject,message){const mailOptions={from:'',to:'',cc,subject,html:message,};transport.sendMail(mailOptions,(error)=>{if(error){console.log(error)}})}
